#ifndef THEIOWACAUCUS_HEADER
#define THEIOWACAUCUS_HEADER

#define p(x, y) (vidmem + (x) + (VIDEO_WIDTH * (y)))

#define pb(x, y) (pixels_back + (x) + (VIDEO_WIDTH * (y)))

int the_iowa_caucus_exit();

#endif // THEIOWACAUCUS_HEADER
